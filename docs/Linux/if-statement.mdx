---
slug: if-statement
title: If statement
authors: [3sam3]
tags: [linux, shell, if, statement]
---

## Code

```sh
#!/bin/bash
input=10

if [ $input -gt 50 ]; then
  echo "UP"
  exit
elif [ $input -eq 10 ]; then
  echo "match"
  exit
else
  echo "down"
  exit
fi
```